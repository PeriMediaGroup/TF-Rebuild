import FeedScreen from "@/components/feed/FeedScreen";

export const metadata = {
  title: "Feed — TriggerFeed",
  description:
    "Catch up on the latest posts from the TriggerFeed community.",
};

export default function FeedPage() {
  return <FeedScreen />;
}
