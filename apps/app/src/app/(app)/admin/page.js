export const metadata = {
  title: "Admin Dashboard — TriggerFeed",
};

export default function AdminPage() {
  return (
    <section>
      <h1>Admin dashboard</h1>
      <p>
        Moderation queues, analytics, ad tooling, and other elevated functions
        from V3 will be recreated in this space.
      </p>
    </section>
  );
}

