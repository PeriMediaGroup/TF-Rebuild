export const metadata = {
  title: "Profile — TriggerFeed",
};

export default function ProfilePage() {
  return (
    <section>
      <h1>Your profile</h1>
      <p>
        Profile management, stats, top friends, and moderation controls will be
        ported from the V3 app into this view.
      </p>
    </section>
  );
}

