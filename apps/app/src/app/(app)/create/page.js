export const metadata = {
  title: "Create Post — TriggerFeed",
};

export default function CreatePostPage() {
  return (
    <section>
      <h1>Create a post</h1>
      <p>
        The full compose flow (media uploads, GIFs, polls, and mentions) will
        live here. We&apos;re wiring it up to match the V3 native experience.
      </p>
    </section>
  );
}

