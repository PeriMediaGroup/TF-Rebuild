export const metadata = {
  title: "Notifications — TriggerFeed",
};

export default function NotificationsPage() {
  return (
    <section>
      <h1>Notifications</h1>
      <p>
        Real-time notifications, friend requests, and moderation alerts from V3
        will surface here in the web build.
      </p>
    </section>
  );
}

